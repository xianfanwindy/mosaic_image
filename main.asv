clc;
clear;
close all;

maindir='.\';
imdir=fullfile(maindir,'data','images');
disp(imdir);
im1=fullfile(imdir,'\1.png');
im2=fullfile(imdir,'\2.png');
im1=imread(im1);
im2=imread(im1);
imshow(im1);